<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F0F0F0">

    <LinearLayout
        android:orientation="vertical"
        android:padding="24dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal">

        <ImageView
            android:id="@+id/imgCep"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:src="@drawable/img_cep"
            android:contentDescription="Imagem CEP" />

        <TextView
            android:text="Consulta de Endereço por CEP"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#333333"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="24dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <EditText
            android:id="@+id/cepInput"
            android:hint="Digite o CEP (somente números)"
            android:inputType="number"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:padding="12dp"
            android:background="@android:drawable/edit_text"
            android:autofillHints="postalCode" />

        <Button
            android:id="@+id/btnBuscar"
            android:text="Buscar Endereço"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:backgroundTint="#1E88E5"
            android:textColor="#FFFFFF" />

        <TextView
            android:id="@+id/resultadoTextView"
            android:text="O endereço aparecerá aqui..."
            android:textSize="16sp"
            android:textColor="#444444"
            android:layout_marginTop="24dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:background="@drawable/bg_resultado" />

    </LinearLayout>
</ScrollView>
